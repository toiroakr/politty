---
"politty": patch
---

Fix new subcommand insertion position in targetCommands mode. Previously, auto-expanded subcommands were appended to the end of the file instead of being inserted at the correct alphabetical position among siblings.
