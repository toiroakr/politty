---
"@politty/valibot": patch
---

Report a missing required argument with the field schema's own message (including a custom one such as `v.string("...")`) instead of valibot's `Invalid key: Expected "<key>" but received undefined`
