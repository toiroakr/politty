---
"politty": patch
---

Update shell completion install instructions for auto-refreshing completions. Bash now points users at the rc loader, zsh recommends the fast fpath setup backed by the managed cache, fish points at `--install`, and `completion zsh --install` no longer prints the slower rc-loader block.
