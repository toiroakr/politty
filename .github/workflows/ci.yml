name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    name: CI
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
      actions: write
      pull-requests: write
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false
      - uses: pnpm/action-setup@26f6d4f2c533a43e6b5da0b4a5dd983f98f7b49a # v6.0.4
        name: Install pnpm
        with:
          standalone: true
          run_install: false
      - name: Install Node.js
        run: pnpm env use --global 24
        shell: bash
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Typecheck, lint, format, and knip
        run: |
          pnpm typecheck &
          TYPECHECK_PID=$!
          pnpm lint &
          LINT_PID=$!
          pnpm format:check &
          FORMAT_PID=$!
          pnpm knip &
          KNIP_PID=$!
          FAIL=0
          for pid in "$TYPECHECK_PID" "$LINT_PID" "$FORMAT_PID" "$KNIP_PID"; do
            wait "$pid" || FAIL=1
          done
          exit $FAIL
      - name: Test with coverage
        run: npx vitest run --coverage --project unit
      - name: Run octocov
        uses: k1LoW/octocov-action@b3b6ee60482a667950f87553abf1df63217235d9 # v1.5.1

  shell-completion:
    name: Shell Completion (${{ matrix.shell }})
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    strategy:
      fail-fast: false
      matrix:
        shell: [bash, zsh, fish]
        include:
          - shell: bash
            install: expect
          - shell: zsh
            install: zsh
          - shell: fish
            install: fish
    steps:
      - name: Install shell tools
        if: matrix.install
        run: |
          sudo apt-get install -y --no-install-recommends ${{ matrix.install }} \
            || (echo "apt-get install failed; updating package lists and retrying." \
                && sudo apt-get update -o Acquire::http::Timeout=10 -o Acquire::Retries=3 \
                && sudo apt-get install -o Acquire::http::Timeout=10 -o Acquire::Retries=3 -y --no-install-recommends ${{ matrix.install }})
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false
      - uses: pnpm/action-setup@26f6d4f2c533a43e6b5da0b4a5dd983f98f7b49a # v6.0.4
        name: Install pnpm
        with:
          standalone: true
          run_install: false
      - name: Install Node.js
        run: pnpm env use --global 24
        shell: bash
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Run shell completion tests
        run: npx vitest run --project shell-${{ matrix.shell }}
