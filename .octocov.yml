# octocov configuration
# https://github.com/k1LoW/octocov

coverage:
  paths:
    - coverage/lcov.info

body:
  enable: true
  hide_footer_link: true

diff:
  datastores:
    - artifact://${GITHUB_REPOSITORY}
