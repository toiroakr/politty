# octocov configuration
# https://github.com/k1LoW/octocov

coverage:
  paths:
    - coverage/lcov.info

body:
  enable: true
  hide_footer_link: false

diff:
  datastores:
    - artifact://${GITHUB_REPOSITORY}
